# 7月迭代 Grilling Session — 待续问题队列

> 已确认决策见 `docs/adr/0001` ~ `docs/adr/0023`。
> 词汇表见根目录 `CONTEXT.md`。
> 评审修订：ADR 0002/0008/0009/0017 已按评审意见修订，新增 ADR 0023 处理审计日志延后。

## 已完成问题

- [x] **问题 6**：资源基础配置页的"优先指派机器人"为**软偏好**（候选池内排序，池空溢出）。已回填 ADR 0006。
- [x] **问题 7**：抢占触发权**分级授权**——紧急到场/作业票监护自动抢占，机会事件人工确认，抢占后强制审计留痕。已写入 ADR 0007。
- [x] **问题 8**：任务状态机扩展为 8 态（新增"待处理""已终止"），已暂停与待处理独立，移除删除按钮只留终止按钮。已写入 ADR 0008。
- [x] **问题 9**：作业票模拟方案——建"第三方系统"模拟页，按钮点击模拟外部推送，联动调度引擎闭环。已写入 ADR 0009。
- [x] **问题 10**：机会事件重新定义为"路过时新发现隐患→告警"，走异常处置流程，砍掉"建议路径优化/主动监护任务"，不新增调度路径。已写入 ADR 0010，并回溯修订 ADR 0007。
- [x] **问题 11**：监护作业人员核对做完整 mock 闭环——作业票带允许人员名单动态推送、模拟页推送识别结果、比对不匹配走异常处置。已写入 ADR 0011。
- [x] **问题 12**：路段级云台扫描配置归路网管理页，边巡边检页保持只读查看；补全 8 个云台配置字段及约束。已写入 ADR 0012。
- [x] **问题 13**：执行端联动三条（抽帧识别流/避空保护/漫游异常锚定）mock 均落调度台，不新建页。已写入 ADR 0013。
- [x] **问题 14**：三项运行策略（限速/心跳频率/语音提醒）都归路网管理页的路段编辑，机器人配置作默认值。已写入 ADR 0014。
- [x] **问题 15**：看门狗阈值固定 0.3~0.4 秒只读外显不可配，触发动作（降速/紧急停车）挂路段，调度台加模拟网络抖动按钮。已写入 ADR 0015。
- [x] **问题 16**：充电点升格为巡检/风险辨识对象但不纳入路线编排，三项指标（转换效率80%/输出功率1kW/实际充电功率800W）阈值固定外显不可配，充电时 mock 监控超阈值告警。已写入 ADR 0016。
- [x] **问题 17**：云台优先执行端自动判断不配；倒车动作树挂路段固定5模板（直行通过/减速通过/停车检查后直行/倒车通过/掉头再进）不让自定义。已写入 ADR 0017。
- [x] **问题 18**：组件维保台账补数据化追踪字段+到期梯度弹窗（30%预警/10%urgent/0%过期），维保项保持扁平不建树。已写入 ADR 0018。
- [x] **问题 19**：故障接管落调度台（可视化展示断点位置+迁移过程+迁移记录），远程校准落控制台配置面板。已写入 ADR 0019。
- [x] **问题 20**：检测项组合算法不改，巡检对象多规则关联天然支持组合形态。已写入 ADR 0020。
- [x] **问题 21**：大模型增强作为检测规则兜底配置（规则级开关），结果按正常格式返回+备注显示"大模型增强"，不建独立解释区块。已写入 ADR 0021。
- [x] **问题 22**：AI 智库独立两页面（知识库管理页文件CRUD+问答页会话列表+对话内容），载体字段区分来源（电脑/机器人），控制台作发起入口，文字输入 mock 语音。已写入 ADR 0022。

## 当前进行中

- 无 — 模块二~六全部功能项已 grilling 完毕

## 待开启问题队列

### 模块二（调度引擎与任务中心）剩余
- [ ] "终止任务"状态审计化的具体状态机迁移（现状"删除任务"改为"终止任务"置停用）
- [ ] "机会事件"的数据契约（识别动火作业/角度优化新看清阀门等触发"建议路径优化"或"主动监护任务"）
- [ ] "动态作业票系统对接"的接口契约（作业票→临时最高优先级区域监护任务的 DAG 强插挂起）
- [ ] "监护作业人员核对"的人脸/安全帽/行为识别落地口径

### 模块三（边巡边检/自由巡检）
- [ ] 配置端改造（边巡边检页）与执行端联动的边界
- [ ] 24帧/秒抽帧识别流控制的 mock 口径
- [ ] 云台空旷区避空保护的策略建模
- [ ] 漫游异常即时锚点的数据契约

### 模块四（运行策略与路网底座）剩余
- [ ] 区域/路况环境自适应策略从机器人级解耦的具体落点
- [ ] 看门狗时效锁机制在 mock 阶段的模拟口径
- [ ] 无线充电点风险化监控的指标建模（转换效率/输出功率/实际充电功率）
- [ ] 云台优先及倒车动作树的动作建模

### 模块五（资产管理与维保迁移）
- [ ] 故障/低电自动唤醒接管的 DAG 断点迁移契约
- [ ] 组件Tab原子动作树维保台账的 Tab 划分边界
- [ ] 远程一键零位校准与标定重载的指令契约

### 模块六（AI智能平台对接）
- [ ] 自训练+大模型组合配置的检测项模型重构
- [ ] 多模态非标隐患解释结论的展示契约
- [ ] 知识库对接+语音双向对讲技术问答的网关契约
