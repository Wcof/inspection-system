# Dashboard 交互缺失审查

## 1. 结论

当前 `dashboard/` 的实现更接近“高保真静态概念稿”，还不是“可联动的巡检首页大屏”。

现有页面的核心问题不是视觉不够，而是：

1. 缺少统一的业务状态模型
2. 缺少地图与辅视角之间的联动机制
3. 缺少任务、机器人、告警、证据、停靠点相关交互
4. 缺少自动轮播与当前聚焦对象切换逻辑

因此，当前版本无法满足“领导通过大屏快速理解当前巡检近况、异常风险、证据和资源状态”的目标。

---

## 2. 当前已存在的交互

当前真正存在的交互非常少，主要只有：

1. 时钟每秒刷新
   - 代码位置：[dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L242)
2. 地图巡检线路的 dash 动画
   - 代码位置：[dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L166)
3. 地图相机持续旋转
   - 代码位置：[dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L119)
4. 告警点脉冲动画
   - 代码位置：[dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L187)

这些都属于“视觉动效”，不属于真正的业务交互。

---

## 3. 关键交互缺失项

### 3.1 地图本身不可交互

当前问题：
- 地图初始化时设置了 `interactive: false`
- 代码位置：[dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L4)

影响：
- 用户不能点击机器人
- 用户不能点击告警点
- 用户不能点击巡检点
- 用户不能通过地图完成任何业务联动

整改要求：
- 地图必须支持基础交互
- 至少支持：
  - 点击机器人
  - 点击巡检点
  - 点击告警点
  - 点击停靠点 / 充电点
- 如果担心领导误操作，可限制拖拽缩放，但不能完全禁止点击

### 3.2 没有“点击机器人 -> 查看当前巡检任务”的交互

当前问题：
- 页面里虽然有“重点任务”和“机器人状态”，但没有任何点击切换逻辑
- HTML 只是固定文案
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L64)
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L86)

缺失后果：
- 无法切换查看不同机器人当前任务
- 无法验证“所有机器人任务近况”
- 无法让地图聚焦某个机器人对应的路线和点位

必须补齐的交互：
- 点击机器人态势列表中的某台机器人
- 点击地图上的机器人点位

交互结果必须同步更新：
- 当前任务主卡
- 地图高亮路径
- 底部任务执行流
- 右侧证据区
- 告警区中与该机器人相关的事件

### 3.3 没有“所有机器人任务总览 + 自动轮播”交互

当前问题：
- 页面只有一个重点任务，没有多机器人任务总览
- 没有自动轮播定时器，没有当前轮播对象状态

缺失后果：
- 用户无法整体了解所有机器人巡检近况
- 页面缺乏“领导站着看，大屏自动讲故事”的能力

必须补齐的交互：
- 自动轮播不同机器人的当前任务
- 手动切换后，可暂停或重置轮播

建议行为：
- 默认每 8-12 秒切换一个执行中机器人
- 高优先级展示顺序：
  - 异常中机器人
  - 执行中机器人
  - 返航中机器人
  - 待命机器人
- 每次切换同步更新：
  - 地图聚焦
  - 任务主卡
  - 证据区
  - 底部执行流
  - 当前机器人状态卡高亮

### 3.4 没有“点击告警 -> 地图定位 -> 证据展示”的交互链路

当前问题：
- 告警列表只是静态文字
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L135)
- 地图只有一个泛化告警点，没有与告警列表绑定
  - [dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L225)

缺失后果：
- 看见异常后，无法知道异常具体在哪
- 看见异常后，无法马上看到对应截图或热视频
- 告警和地图、视频之间完全脱节

必须补齐的交互：
- 点击告警列表项

交互结果必须同步更新：
- 地图聚焦到对应异常设备 / 巡检点
- 高亮异常锚点
- 切换右侧证据展示区为该异常的截图 / 视频
- 高亮关联任务和关联机器人

### 3.5 没有“证据模式”和“视频模式”的切换交互

当前问题：
- 右侧双光视频区只是两个静态 mock
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L117)
- 没有证据优先模式
- 没有可见光 / 热红外主次切换
- 没有问题片段预览

缺失后果：
- 视频模块无法承担业务解释职责
- 领导看不出“这个问题是否真实存在”

必须补齐的交互：
- 证据模式 / 实时模式切换
- 可见光 / 热红外切换
- 问题截图 / 问题视频切换

建议行为：
- 默认无异常时展示实时双光画面
- 选中异常后自动切换为证据模式
- 证据模式下突出：
  - 问题截图
  - 时间
  - 巡检点
  - 设备
  - 机器人
  - 异常类型

### 3.6 没有“巡检视角是否偏离目标”的交互反馈

当前问题：
- 页面没有任何“目标设备”和“镜头朝向”的表达
- 视频与地图之间也没有当前检测目标的对应关系

缺失后果：
- 用户无法判断机器人是否真的在看该看的设备
- 无法回答“巡检是否偏离目标”

必须补齐的交互：
- 当前检测目标显示
- 当前镜头视角状态显示
- 目标对准状态反馈

建议行为：
- 页面显示：
  - 当前目标设备
  - 当前目标点位
  - 当前视角状态：已对准 / 轻微偏移 / 严重偏离
- 地图上增加机器人朝向或视角扇区
- 当视角偏离时，视频区和地图区同时提示

### 3.7 没有“停靠点 / 充电点”的点击和联动交互

当前问题：
- 页面没有对应模块
- 地图没有停靠点和充电点对象图层

缺失后果：
- 无法回答“有没有停靠点，它们分布在哪里”
- 无法知道哪些点位被占用、哪些可用

必须补齐的交互：
- 点击停靠点 / 充电点查看详情

交互结果建议：
- 地图高亮对应点位
- 辅视角显示：
  - 点位名称
  - 所属区域
  - 状态：空闲 / 占用 / 故障
  - 当前占用机器人

### 3.8 底部时间轴缺少“任务执行流切换”交互

当前问题：
- 底部只是一条静态时间线
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L187)

缺失后果：
- 无法随着机器人切换显示不同任务流
- 无法点击节点查看某个巡检点详情
- 无法表达“当前点 / 下一点 / 异常点”

必须补齐的交互：
- 底部时间轴根据当前聚焦任务切换
- 节点支持点击查看点位详情

交互结果建议：
- 点击某个巡检点节点后：
  - 地图聚焦该点
  - 右侧显示该点位的设备检测结果
  - 如有异常，切换到证据区

### 3.9 顶部缺少“场站 / 设备上下文切换”的交互表达

当前问题：
- 顶栏只有标题、环境和时间
  - [dashboard/index.html](/Users/ldh/Downloads/project/极客光年/bot/dashboard/index.html#L15)
- 没有体现当前场站 / 当前设备上下文切换

缺失后果：
- 页面缺乏 PRD 要求的上下文信息
- 多厂区、多机器人场景下用户不清楚当前看的是什么

必须补齐的交互：
- 场站切换
- 当前设备或当前聚焦机器人上下文展示

如果不做真实下拉，也至少要有明确的当前上下文卡片。

### 3.10 相机持续旋转不适合业务联动

当前问题：
- 相机一直自动旋转
  - [dashboard/app.js](/Users/ldh/Downloads/project/极客光年/bot/dashboard/app.js#L119)

缺失后果：
- 用户难以保持空间方位感
- 一旦做任务切换、异常定位、视角确认，持续旋转会让地图可读性下降

整改要求：
- 不要持续无条件旋转
- 改为：
  - 默认微动
  - 切换任务时短暂镜头过渡
  - 选中异常或机器人时定点聚焦

---

## 4. 必须补齐的最小交互闭环

当前页面至少要补齐以下 5 条交互闭环，才算进入“可用的大屏”阶段。

### 闭环 1：机器人 -> 任务
- 点击机器人态势卡或地图机器人
- 更新当前任务主卡
- 高亮该机器人路径和当前巡检点

### 闭环 2：任务 -> 执行流
- 切换当前任务
- 底部执行流同步切换
- 当前点、下一点、异常点更新

### 闭环 3：告警 -> 地图 -> 证据
- 点击告警列表
- 地图定位异常点
- 证据区切换到异常截图 / 视频

### 闭环 4：轮播 -> 全局联动
- 自动轮播不同机器人任务
- 同步切换地图、任务、证据、执行流

### 闭环 5：巡检视角 -> 目标确认
- 显示当前检测目标
- 显示镜头是否对准目标
- 若偏离，给出提示

---

## 5. 推荐的页面状态模型

为了补齐交互，前端必须建立统一状态，而不是继续写死 HTML。

建议至少存在以下状态：

```js
currentRobotId
currentTaskId
currentAlertId
currentPatrolPointId
currentDockPointId
currentEvidenceMode
currentCameraMode
autoplayEnabled
autoplayIndex
```

建议至少存在以下数据集合：

```js
robots[]
tasks[]
alerts[]
patrolPoints[]
dockPoints[]
chargePoints[]
evidenceAssets[]
taskTimeline[]
```

---

## 6. 给 Gemini 的直接要求

请 Gemini 基于当前 `dashboard/` 页面补齐交互，不要只做视觉改版。

必须完成：

1. 给地图增加基础点击联动能力
2. 增加机器人、巡检点、停靠点、充电点、告警点的业务图层
3. 增加“所有机器人任务近况”和“自动轮播任务”
4. 增加“点击告警 -> 地图定位 -> 证据展示”链路
5. 增加“巡检视角是否偏离目标”的状态表达
6. 将底部抽象时间轴改为“可切换的任务执行流”
7. 停止把页面写成静态文案堆砌，改为状态驱动的联动大屏

---

## 7. 验收标准

整改完成后，页面必须满足：

1. 点击任一机器人，页面能切换到该机器人当前任务视角
2. 页面能自动轮播不同机器人任务，并同步更新所有关联区域
3. 点击任一异常，地图和证据区能联动
4. 页面能看到停靠点 / 充电点资源并支持查看详情
5. 页面能判断巡检视角是否对准目标
6. 地图不会因持续旋转而破坏业务可读性
7. 页面仍保持高科技、高级、领导大屏气质，但不再是静态概念图
