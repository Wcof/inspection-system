# 安全生产巡检任务管理系统 — 项目配置

本文件是 AtomCode 项目级指令配置，整合自 `AGENTS.md` 和 `CLAUDE.md`。

## 开发命令

| 场景 | 命令 |
|------|------|
| 安装依赖 | `npm install` |
| 开发服务器 | `npm run dev` |
| 构建（默认 /） | `npm run build` （运行 `vue-tsc && vite build`） |
| 构建（v2 路径） | `npm run build:v2` （输出到 `/inspection-system/v2/`） |
| 预览构建 | `npm run preview` |
| 类型检查 | `npx vue-tsc --noEmit` |
| 仪表盘子项目 | `cd dashboard && npm run build` |

## 项目架构

### 概述

**安全生产巡检任务管理系统** — 一个 Vue 3 SPA，用于管理工业环境中的自主巡检机器人。

两个界面通过顶部 radio 切换：
- **管理端** — 调度台、任务管理、异常处理、报表统计
- **实施端** — 地图管理、机器人配置、巡检点采集、设备设施设置、检测规则配置

### 技术栈

- Vue 3 + TypeScript（Composition API + `<script setup>`）
- Pinia 状态管理
- Ant Design Vue 4（通过 `unplugin-vue-components` 自动导入）
- Vue Router 4（hash 模式 `createWebHashHistory`）
- Vite 6 打包
- Sass 自定义样式
- 路径别名：`@` → `./src`

### 数据层 — 无后端

所有数据存储在 `localStorage` 中，通过 `MockService`（位于 `src/mock/mockService.ts`）操作。
模式：Pinia stores 调用 `MockService.getXxx()` 读取、`MockService.saveXxx()` 写入。
ID 生成：`Date.now()` 前缀字符串（如 `point-1681234567890`）。
数据迁移：`src/mock/migrations.ts`。

### 两个项目根

1. **根目录 `/`** — 主 Vue 3 + Vite SPA
2. **`dashboard/`** — 独立的纯 JS + MapLibre GL 仪表盘（实时机器人监控指挥中心），独立 `package.json`

### 关键目录结构

```
src/
├── types/           # TypeScript 接口与枚举
│   ├── common.ts    # 共享类型
│   ├── robot.ts     # Robot, RobotStatus
│   ├── inspection.ts # 核心领域类型
│   ├── exception.ts # 异常类型
│   └── path.ts      # InspectionPath
├── stores/          # Pinia stores
├── mock/            # MockService + initialData + migrations
├── components/      # 共享布局组件
├── views/           # 页面组件
│   ├── inspection/  # 巡检点、计划、任务、设备
│   ├── management/  # 调度台、任务、异常、报表
│   ├── implementation/ # 检测项、调度规则、指标、校准
│   ├── map/         # 地图编辑器、点位管理、区域管理
│   └── robot/       # 机器人 CRUD
├── router/          # 路由定义
└── utils/           # Storage 工具
```

### 领域模型（核心实体）

- **InspectionPoint** — 地图上的物理巡检位置，含停车点（parkingPoints）和采集姿态（collectionPoses）
- **InspectionDevice** — 巡检点处的设施/设备，含 assetComponents、checkItems 等
- **InspectionTask** — 调度或一次性执行任务，引用机器人、路线和巡检点
- **InspectionPlan** — 周期性排班计划（周/月/一次），生成任务实例
- **InspectionMap / Waypoint / WaypointEdge** — 地图图结构
- **InspectionRoute** — 有序的路径点和巡检点序列
- **StandardComponent** — 标准库中的可复用组件模板

### 路由结构

嵌套在两条顶层路径下：`/management/*` 和 `/implementation/*`。
`AppLayout` 组件的侧边栏菜单根据当前端切换。默认路由 `/` 重定向到 `/management/dispatch/center`。

### CI/CD

GitHub Actions（`.github/workflows/deploy-pages.yml`）部署到 GitHub Pages。
构建生成两个版本：v1（根路径）、v2（`/inspection-system/v2/`），合并到同一 `dist/` 产物。

## 工具链配置

### CodeGraph（代码知识图谱）

项目中已有 `.codegraph/` 目录，包含 SQLite 数据库（`codegraph.db`）。
使用方式：CodeGraph 会自动索引项目代码结构，辅助代码定位和理解。
该目录已配置 `.gitignore`，数据文件不提交到版本控制。

### RTK（Runtime Toolkit）

当前项目未安装 RTK 工具链。如果需要使用，可参考以下安装方式：
```bash
# npm 全局安装
npm install -g rtk

# 使用方式（所有 shell 命令加 rtk 前缀）
rtk npm run dev
rtk npx vue-tsc --noEmit
```

### Ponytail

当前项目中未检测到 Ponytail 相关配置或依赖。

## PRD Helper

本项目使用 PRD Helper Skill Kit 处理产品上下文。
- 完整规则见 `support/adapters/canonical-rules.md`
- 斜杠命令：`/prd-start`、`/prd-stop`、`/prd-pause`、`/prd-resume`、`/prd-status`、`/prd-remove`
